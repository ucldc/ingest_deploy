---
- hosts: localhost
  connection: local
  gather_facts: yes
  vars:
    role: majorTom
    dir_role_templates: "roles/{{ role }}/templates"
  roles:
    - { role: common, sudo: no }
    - { role: worker, sudo: no }
    - { role: majorTom, sudo: no }
